<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>{{ title }}</h1>
    <p>Welcome to {{ title }}</p>

    <div class="rooms">
        {{#roomMap}}
            <span class="room">
                <a href="/rooms/{{room}}/toggle">{{ display }}</a>
            </span>
        {{/roomMap}}
    </div>
  </body>
</html>
